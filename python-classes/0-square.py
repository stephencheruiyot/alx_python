class Square:
    """A class that defines a square by its size.
    
    
   Attributes: 
    -__size (int) : The size of the square(side lenght).
    """
    def __init__(self, size):
        """
        Initializes an instance of `Square` with given size.

        Args:
            size (int):The size of the square (side length). 
        """
        self.__size = size
            
